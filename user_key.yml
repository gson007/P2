---
- hosts: invent
  remote_user: godsonan
  become: True
  tasks:
   - name: create user with key
     user:
      name: lytsvc
      generate_ssh_key: yes
      ssh_key_bits: 2048
      ssh_key_file: .ssh/id_rsa